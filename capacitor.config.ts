import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.isymbolic.lavaleap',
  appName: 'Lava Leap',
  webDir: 'dist',
  backgroundColor: '#10101a',
  android: {
    backgroundColor: '#10101a',
  },
};

export default config;
